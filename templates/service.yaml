apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctnginx.fullname" . }}
  labels:
    {{- include "ctnginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ctnginx.selectorLabels" . | nindent 4 }}
